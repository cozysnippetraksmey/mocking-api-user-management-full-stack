export const environment = {
  production: true,
  apiUrl: 'http://api.localhost',  // Traefik routing URL
  apiVersion: 'v1',
  features: {
    enableToast: true,
    enablePagination: true,
    defaultPageSize: 10
  }
};

