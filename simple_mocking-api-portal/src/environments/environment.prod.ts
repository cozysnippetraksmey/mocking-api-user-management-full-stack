export const environment = {
  production: true,
  apiUrl: 'https://your-api-domain.com',
  apiVersion: 'v1',
  features: {
    enableToast: true,
    enablePagination: true,
    defaultPageSize: 10
  }
};

